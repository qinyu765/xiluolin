{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the main and fallback result windows.",
  "windows": ["main", "fallback-result"],
  "permissions": [
    "opener:default",
    "sql:default",
    "store:default",
    "core:event:allow-listen",
    "core:event:allow-emit"
  ]
}
