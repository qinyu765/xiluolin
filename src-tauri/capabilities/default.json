{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the main and recording indicator windows.",
  "windows": ["main", "recording-indicator"],
  "permissions": [
    "opener:default",
    "sql:default",
    "store:default",
    "core:event:allow-listen",
    "core:event:allow-emit"
  ]
}
