# XiLuoLin 故障排查

本文档记录当前版本可执行的故障定位和恢复方法。历史原因分析和旧实现细节见 [`dev/archive/fixes/`](./dev/archive/fixes/)。

## 先做基础检查

1. 确认麦克风已连接，并在系统设置中授予应用麦克风权限。
2. 在“设置 → 模型配置”中确认 ASR 与文本处理 Provider、Base URL、模型和 API Key 已保存。
3. 确认全局快捷键没有被操作系统或其他应用占用。
4. 在目标输入框中重新放置光标，再开始一次短录音。
5. 记录操作系统、应用 commit、复现步骤和脱敏后的错误信息。

## 无法开始或停止录音

### 麦克风无响应

- 在系统声音设置中确认输入设备可用，并将其设为当前输入设备。
- 关闭占用麦克风的会议或录音软件后重试。
- 重新授予麦克风权限；修改权限后通常需要重启应用。
- 若设备刚插入或切换，重新打开 XiLuoLin 后再测试。

### 快捷键无响应或冲突

- 在设置页重新录制快捷键，避免系统快捷键和常用软件组合键。
- 分别测试长按和切换模式，确认按键释放事件能够被系统接收。
- 使用左右修饰键时，以设置页实际显示和保存的组合为准。
- 若应用以不同权限级别运行，先让 XiLuoLin 与目标应用使用相同权限级别。

### 录音结束后没有继续处理

- 确认录音期间没有切换或关闭主窗口。
- 检查界面是否提示麦克风、临时音频或 Provider 错误。
- 先用 5 至 15 秒短语音复现，排除长音频和网络超时影响。
- 开发环境可检查 `recording-completed`、`recording-error` 和 pipeline 相关日志，但提交日志前必须脱敏。

## Provider 调用失败

### 未配置或认证失败

- 确认 ASR 和文本整理使用了各自正确的 Provider 与 API Key。
- 检查 Base URL 是否属于所选 Provider，模型名是否受该服务支持。
- 重新保存配置后再测试，不要把 API Key 写入仓库、Issue 或截图。

### 超时、限流或服务异常

- 检查网络、代理和 Provider 服务状态。
- 使用短录音重试，避免同时发起多次处理。
- 保留 HTTP 状态码和脱敏后的错误摘要，不记录请求中的音频、完整文本或认证头。
- 若使用兼容 OpenAI 协议的服务，确认端点和响应结构与当前 Provider 实现兼容。

## 文本没有输出到目标应用

XiLuoLin 当前优先把结果写入剪贴板并模拟粘贴；自动粘贴失败时，至少保留剪贴板内容供手动粘贴。

### 输出到了错误位置

- macOS 和 Windows 会记录录音开始时的目标窗口；如果系统不能确认恢复成功，将取消自动按键并保留剪贴板文本。
- 开始录音后可以切换窗口，但不要退出原目标应用。
- 开始录音前先点击目标输入框，确保光标可见。
- 在记事本或其他普通文本编辑器中复现，排除目标应用自身限制。

### 自动粘贴失败

- 尝试手动执行 `Ctrl+V`；macOS 使用 `Command+V`。
- macOS 打开“设置 → 语音输入就绪检查”，确认辅助功能权限已授权；必要时使用“打开辅助功能设置”。
- 检查目标应用是否禁止模拟键盘、剪贴板或自动化操作。
- Windows 下，低权限进程通常不能向高权限窗口注入输入；让两个应用使用相同权限级别。
- 检查安全软件、远程桌面或沙盒是否拦截剪贴板和模拟按键。

### 出现重复文本或字符

- 确认只启动了一个 XiLuoLin 实例。
- 在普通文本编辑器中重试，判断问题是否只发生在特定应用。
- 检查前端事件监听是否被重复注册，以及组件卸载时是否正确清理监听器。
- 当前输出链路应以剪贴板粘贴为主，不应同时执行多套自动输出路径。

## 历史、统计或设置未保存

- 正常退出并重新启动应用，确认问题是否稳定复现。
- 检查应用数据目录和系统凭据库是否可写。
- API Key 保存在系统凭据库；普通设置和业务数据使用本地存储，不应通过手工编辑旧版 `settings.json` 注入密钥。
- 数据库或凭据迁移失败时，保留脱敏错误，不要直接删除用户数据。

## 开发环境检查

按改动范围运行：

```bash
pnpm typecheck
pnpm build
pnpm check
```

涉及桌面能力时运行 `pnpm tauri dev`，并在目标操作系统上手动验证麦克风、全局快捷键、Provider 和跨应用粘贴。Apple Silicon 安装包使用 `pnpm tauri:build:macos:arm64`，详见 [`macos-build.md`](./macos-build.md)。

## 仍无法解决

提交 Issue 时提供：

- 操作系统、版本和 CPU 架构。
- XiLuoLin 版本或 commit。
- 最小复现步骤、预期行为和实际行为。
- 已运行的检查命令。
- 脱敏后的错误信息。

不要提交 API Key、私人录音、用户完整文本或完整本地路径。安全问题按照 [`../SECURITY.md`](../SECURITY.md) 私下报告。
