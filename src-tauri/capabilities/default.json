{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the main window.",
  "windows": ["main"],
  "permissions": ["opener:default", "sql:default", "store:default"]
}
